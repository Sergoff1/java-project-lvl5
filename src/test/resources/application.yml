spring:
  datasource:
    url: jdbc:h2:mem:db
    username: sa
    password:

  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml

  jackson:
    serialization:
      fail-on-empty-beans: false